vswarte
> Decision tree defines the argument states in a list and the nodes depth matches the argument. Node keys need to be sorted by id from low to high on the output. From there it's (for the time being) reusing one of the reserved slots in the conversion params for BgmEnemyType and EnvPlaceType. 

